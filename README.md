# Luyoung shell

Low-key static shell page built with plain HTML/CSS/JavaScript.

## Commands

```bash
npm install
npm run start
npm run validate
```

Open `http://localhost:4173/` after starting the server.

Inside the shell:

```bash
posts
repos
open post la
open repo cpu
close
```
